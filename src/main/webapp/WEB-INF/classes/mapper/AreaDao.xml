<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.house.dao.AreaDao">
    <select id="queryArea" resultType="com.house.entity.Area">
    SELECT
    id, area_name, create_time, update_time
    FROM
    area
    </select>

</mapper>